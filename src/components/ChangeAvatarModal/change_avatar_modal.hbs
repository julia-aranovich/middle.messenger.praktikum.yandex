<div>
  <div id="change-photo-modal-background" class="{{#if isVisible}}visible{{else}}hidden{{/if}}"></div>
  <div id="change-photo-modal" class="{{#if isVisible}}visible{{else}}hidden{{/if}}">
    <form>
      <div class="field">
        <h2>Загрузите фото</h2>
      </div>
      <div class="field">
        <a>Выберите фото на компьютере</a>
        <input type="file" name="avatar" />
      </div>
      {{{submitButton}}}
    </form>
  </div>
</div>
